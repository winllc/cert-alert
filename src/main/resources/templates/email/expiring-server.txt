[# th:if="${digest.recipientName}"]Hello [(${digest.recipientName})],[/][# th:unless="${digest.recipientName}"]Hello,[/]

[(${digest.headline})].

You are a point of contact for [# th:if="${digest.total} == 1"]a server whose certificate needs[/][# th:unless="${digest.total} == 1"]servers whose certificates need[/] attention.
Other people may be contacts for the same servers, so somebody may be on this already.
[# th:each="entry : ${digest.entries}"]
  * [(${entry.subjectDn})]
    Server [(${entry.ownerName})] ([(${entry.ownerDn})])
    [# th:if="${entry.expired}"]Expired [(${entry.daysAbsolute})] day(s) ago, on [(${entry.day})][/][# th:unless="${entry.expired}"]Expires in [(${entry.days})] day(s), on [(${entry.day})][/]
    [(${entry.keyDescription})][# th:if="${entry.hashAlgorithm}"] / [(${entry.hashAlgorithm})][/], issued by [(${entry.issuerDn})]
[/]
You are receiving this because you are a point of contact for these servers.

This is the daily round-up from cert-alert. It reports what the directory publishes: a
certificate stops appearing here once the renewed one is published in its place.

spring:
  datasource:
    url: jdbc:h2:mem:cert-alert-test;DB_CLOSE_DELAY=-1
    username: sa
    password: ""
    driver-class-name: org.h2.Driver

cert-alert:
  ldap:
    # Tests drive the sync explicitly; nothing should fire on a timer.
    sync-enabled: false

logging:
  level:
    com.winllc.certalert: DEBUG
